# GDKata

A Godot 4.5+ Editor Plugin for practicing GDScript through coding katas.

## What is a Kata?

A kata is a small coding exercise designed to improve your programming skills through practice. Each kata presents a problem that you solve by implementing a GDScript function. Your solution is automatically tested against predefined test cases.

## Features

- **Dock-First Workflow** - No extra main-screen tab, optimized for Godot's script editor flow
- **128+ Coding Challenges** - From basic algorithms to design patterns
- **Unified Kata JSON** - Status and i18n live in a single kata file
- **Integrated Test Runner** - Run tests and inspect detailed results in the right dock
- **Progress Tracking** - `todo / in_progress / done` status is stored directly in each kata JSON
- **Difficulty Levels** - Challenges range from beginner to advanced

## Installation

**Not yet available in the AssetLib**

### Manual Installation

1. Download or clone this repository
2. Copy the `addons/gdkata` folder into your project's `addons` directory
3. Enable the plugin in **Project > Project Settings > Plugins**

## Usage

1. Enable the plugin and use the two docks:
   - **Left:** Kata selection, filters, hints
   - **Right:** Run tests, inspect results, submit/cancel active kata
2. Select a kata and click **Start Kata**
3. Implement the required function in the generated script file
4. Run tests in the right dock
5. Submit with **Submit completed Kata** once all tests pass

## Kata Categories

### Basic Algorithms
- String manipulation (reverse, count vowels, palindrome check)
- Array operations (sum, filter, find maximum)
- Math functions (factorial, fibonacci, prime check)

### Intermediate
- Sorting algorithms (Bubble Sort, Merge Sort, Quick Sort)
- Search algorithms (Binary Search, Dijkstra)
- Data structures (LRU Cache)

### Design Patterns
- Creational: Singleton, Factory, Builder
- Structural: Composite, Decorator
- Behavioral: Observer, Strategy, Command, State Machine, and more

## Project Structure

```
addons/gdkata/
├── core/               # Plugin core functionality
│   ├── definitions/    # Data classes for katas
│   ├── docks/          # UI panels (Selector, Results)
│   └── ui_components/  # Reusable UI elements
├── katas/
│   ├── catalog/        # Unified kata JSON files (status + i18n)
│   ├── workspace/      # Active script + last result
│   └── ...
├── plugin.cfg          # Plugin configuration
└── gdkata.gd           # Main plugin script
```

## Creating Custom Katas

Create a JSON in `addons/gdkata/katas/catalog/` with:

- `id`: stable snake_case identifier
- `status`: `todo`, `in_progress`, or `done`
- `method_name`, `expected_type_hint`, `difficulty`, `arguments`, `tests`
- `translations`: locale dictionary (`en`, `de`, ...) with `name`, `description`, `hints` and optional localized test labels

If you still have an old project state with `1_todo/2_in_progress/3_done`, the plugin can migrate it to `catalog/`.

### Legacy Migration (How it works)

Migration runs automatically on plugin startup (`_enter_tree`) via `GDKataDefinition.ensure_catalog_initialized()`.

It runs only when `addons/gdkata/katas/catalog/` does not contain any JSON files yet.

During migration, the plugin:

1. Reads legacy katas from:
   - `1_todo/*.json`
   - `2_in_progress/*.json`
   - `3_done/*.json`
   - `3_done/*/kata.json`
2. Merges language variants of the same kata into one unified entry (based on method/type/difficulty/arguments/test inputs+expected values).
3. Writes one file per kata to `katas/catalog/<kata_id>.json` with:
   - `status` (`todo`, `in_progress`, `done`)
   - `translations` (`en`, `de`, ...)
   - normalized tests/arguments metadata.
4. Copies the first legacy in-progress script (`2_in_progress/*.gd`) into `katas/workspace/` if present.

Important:
- Legacy folders are not deleted automatically.
- If `catalog/` already contains JSON files, migration is skipped.

## Contributing

Contributions are welcome! Feel free to:
- Add new katas
- Improve existing kata descriptions
- Fix bugs
- Translate katas to other languages

## License

MIT License - See LICENSE file for details.

## DISCLAIMER

The initial Proof of Concept was 100% implemented by me. I used AI for the following things:

- Refactoring of the testrunner (previously it was a scene that you had to start, as doing Katas this way felt clunky, I wanted to change that to a testrunner that's also a @tool-script)
- i18n inside the editor. Godot uses different translation mechanisms whether it's in editor or in runtime context. After fiddling around for more time than I was willing to spend, I told AI to do it (`./addons/gdkata/core/gdkata_tr.gd` - if anyone is interested)
- Minor improvements of UI/UX
- **Content**! Almost every single Kata was generated by AI and I actually think that it's a nice use case to let AI generate further Katas that you have to solve as you probably already know the solution if you'd need to write Katas for yourself that you solve afterwards. **This also means:** I haven't tested every Kata! Feel free to correct it or throw it away and write your own :)

_I know, using AI for code generation has its drawbacks, but this addon just serves the purpose to learn GDScript. I'd personally not be able to invest 2 weeks or more into this project to polish and improve everything by myself. I also did review the code and I consider it "fine enough" for the purpose of the addon, but feel free to replace the sloppy parts with something more maintainable and/or cleaner code._
